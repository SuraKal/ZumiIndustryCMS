name: Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  web-deploy:
    name: Deploying
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: shivammathur/setup-php@15c43e89cdef867065b0213be354c2841860869e
        with:
          php-version: '8.2.4'

      - name: Install PHP dependencies
        run: composer install --no-dev --prefer-dist --ignore-platform-reqs

      - name: Copy .env
        run: php -r "file_exists('.env') || copy('.env.production', '.env');"

      - name: 🔨 Build Frontend (NPM)
        run: |
          npm install
          npm run build

      - name: Generate key
        run: php artisan key:generate

      - name: Directory Permissions
        run: chmod -R 775 storage

      - name: Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.SERVER }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          exclude: |
            **/node_modules/**
            **/vendor/**
            **/public/storage/**

      # - name: Database Migrations
      #   run: php artisan migrate:fresh --seed

# SSH TO THE FILE FOLDER AND RUN:
# ln -sfn /home/naelprks/laravel_project_files/pure_photography/storage/app/public /home/naelprks/pure.naelproduction.com/storage